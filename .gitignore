# Dependencies — reinstalled via package.json, no need to version them
node_modules/

# Build output — regenerated by `npm run build`, not source code
dist/
build/

# Env files, if this frontend ever gets its own
.env
.env.*
!.env.example

# Anything zipped up for manual uploads
*.zip

# OS/editor noise
.DS_Store
Thumbs.db
